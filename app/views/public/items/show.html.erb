items/show

<%= form_with model: @item, url: 'admin_items', method: :get do |f| %>
<% end %>
  <div class="mx-auto" style="width: 800px">
               <div class="container">
                 <span class="text-light bg-secondary px-3">商品詳細</span>
            　 </div>
             </div>
  <div class="container">
  <div class="row align-items-center" style="height:50px">
 
    <div class="col">
   <div><% if @item.image.attached? %>
   <%= image_tag @item.image, size: "200x200" %>
<% else %>
  <%= image_tag 'no_image', size: "200x200" %>
<% end %>
</div>
    </div>
  </div>
</div>
<div class="mx-auto" style="width: 400px">
  <div>商品名　　<%= @item.name %></div>
  <div>商品説明　　<%= @item.introduction %></div>
  <div>税込価格（税抜価格）　<%= @item.price %><%= @item.price %>円</div>
  <div>販売ステータス　
  <% if @item.is_active==true %>
  　　販売中
  <% else %>
  　　販売停止中
  <% end %>
  </div>
  </br>
  <div class="mx-auto" style="width: 200px">
  <div class="action">
    <div style="text-align:center">
    <%= link_to "カートに入れる",  edit_admin_item_path(@item.id) ,class: "btn btn-success" %>
  </div>
  </div>
</div>
</div>