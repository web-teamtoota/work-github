<%= form_with model: @item, url: 'admin_items', method: :get do |f| %>
<% end %>
  <div class="mx-auto" style="width: 800px">
     <div class="container">
       <span class="text-light bg-secondary px-3">商品詳細
       </span>
     </div>
  </div>

  <div class="container">
  <div class="row align-items-center" style="height:50px">
 
    <div class="col">
   <div><% if @item.image.attached? %>
   <%= image_tag @item.image, size: "200x200" %>
<% else %>
  <%= image_tag 'no_image', size: "200x200" %>
<% end %>
</div>
    </div>
  </div>
</div>
<div class="mx-auto" style="width: 400px">
  <div>商品名　　　　<%= @item.name %></div>
  <div>商品説明　　　<%= @item.introduction %></div>
  <div>税込価格</br>
（税抜価格）　　　<%= @item.with_tax_price.to_s(:delimited) %>　(<%= @item.price.to_s(:delimited) %>)円
 </div>
  <div>販売ステータス　
  　<% if @item.is_active==true %>
   <div class="text-success">　　　　　　　　　　販売中
  <% else %>
  <p class="text-secondary">　　　　　　　　　　販売停止中</p> 
  <% end %>
  </div>
  </br>
  
  <div class="mx-auto" style="width: 200px">
    <div style="text-align:center">
    <%= link_to "編集する",  edit_admin_item_path(@item.id) ,class: "btn btn-success" %>
  </div>
  </div>
</div>